name: scanner
description: A new Flutter package project.
version: 3.0.2
homepage: https://github.com/fanchungchit/scanner

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  collection: ^1.17.0
  flutter:
    sdk: flutter
  honeywell_scanner: ^5.0.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
